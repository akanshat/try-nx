import { Task, TaskGraph } from '@nrwl/devkit';
import { Hash, Hasher } from '@nrwl/workspace/src/core/hasher/hasher';
export default function run(task: Task, taskGraph: TaskGraph, hasher: Hasher): Promise<Hash>;
