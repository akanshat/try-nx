import { WebpackConfigOptions } from '../build-options';
export declare function getBrowserConfig(wco: WebpackConfigOptions): {
    devtool: string | boolean;
    resolve: {
        mainFields: string[];
    };
    output: {
        crossOriginLoading: string | boolean;
    };
    optimization: {
        runtimeChunk: string | boolean;
        splitChunks: {
            maxAsyncRequests: number;
            cacheGroups: {
                default: {
                    chunks: string;
                    minChunks: number;
                    priority: number;
                };
                common: {
                    name: string;
                    chunks: string;
                    minChunks: number;
                    enforce: boolean;
                    priority: number;
                };
                vendors: boolean;
                vendor: {
                    name: string;
                    chunks: string | ((chunk: any) => boolean);
                    enforce: boolean;
                    test: RegExp | ((module: {
                        nameForCondition?: Function;
                    }, chunks: Array<{
                        name: string;
                    }>) => boolean);
                };
            };
        };
    };
    plugins: any[];
    node: boolean;
};
