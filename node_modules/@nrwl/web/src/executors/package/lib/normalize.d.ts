import { AssetGlobPattern } from '../../../utils/types';
import { WebPackageOptions } from '../schema';
export interface NormalizedWebPackageOptions extends WebPackageOptions {
    entryRoot: string;
    projectRoot: string;
    assets: AssetGlobPattern[];
    rollupConfig: string[];
}
export declare function normalizePackageOptions(options: WebPackageOptions, root: string, sourceRoot: string): NormalizedWebPackageOptions;
