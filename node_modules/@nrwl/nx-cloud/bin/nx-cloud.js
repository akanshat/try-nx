#!/usr/bin/env node
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const distributed_agent_runner_1 = require("../lib/core/runners/distributed-agent/distributed-agent.runner");
const start_ci_run_1 = require("../lib/core/commands/start-ci-run");
const stop_all_agents_1 = require("../lib/core/commands/stop-all-agents");
const clean_up_agents_1 = require("../lib/core/commands/clean-up-agents");
const command = process.argv[2];
if (command === 'start-agent') {
    distributed_agent_runner_1.startAgent().catch((e) => {
        console.error(e);
        process.exit(1);
    });
}
else if (command === 'stop-all-agents') {
    stop_all_agents_1.stopAllAgents().catch((e) => {
        console.error(e);
        process.exit(1);
    });
}
else if (command === 'start-ci-run') {
    start_ci_run_1.startCiRun().catch((e) => {
        console.error(e);
        process.exit(1);
    });
}
else if (command === 'clean-up-agents') {
    clean_up_agents_1.cleanUpAgents().catch((e) => {
        console.error(e);
        process.exit(1);
    });
}
else {
    console.log('Pass start-agent, stop-all-agents, or clean-up-agents');
}
//# sourceMappingURL=nx-cloud.js.map