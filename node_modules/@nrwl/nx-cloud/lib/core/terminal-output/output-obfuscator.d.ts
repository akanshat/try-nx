export declare class OutputObfuscator {
    private maskedProperties;
    constructor();
    obfuscate(terminalOutput: string): string;
    toCamelCase(input: string): string;
    private loadSecretKeys;
}
