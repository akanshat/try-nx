"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.formatFiles = void 0;
const tslib_1 = require("tslib");
const schematics_1 = require("@angular-devkit/schematics");
const rxjs_1 = require("rxjs");
const operators_1 = require("rxjs/operators");
const path = require("path");
const app_root_1 = require("@nrwl/tao/src/utils/app-root");
const workspace_1 = require("@nrwl/tao/src/shared/workspace");
const devkit_1 = require("@nrwl/devkit");
function formatFiles(options = { skipFormat: false }, directory = '') {
    let prettier;
    try {
        prettier = require('prettier');
    }
    catch (e) { }
    if (options.skipFormat) {
        return schematics_1.noop();
    }
    return (host, context) => {
        updateWorkspaceJsonToMatchFormatVersion(host, directory);
        if (!prettier) {
            return host;
        }
        const files = new Set(host.actions
            .filter((action) => action.kind !== 'd' && action.kind !== 'r')
            .map((action) => ({
            path: action.path,
            content: action.content.toString(),
        })));
        if (files.size === 0) {
            return host;
        }
        return rxjs_1.from(files).pipe(operators_1.filter((file) => host.exists(file.path)), operators_1.mergeMap((file) => tslib_1.__awaiter(this, void 0, void 0, function* () {
            const systemPath = path.join(app_root_1.appRootPath, file.path);
            let options = {
                filepath: systemPath,
            };
            const resolvedOptions = yield prettier.resolveConfig(systemPath);
            if (resolvedOptions) {
                options = Object.assign(Object.assign({}, options), resolvedOptions);
            }
            const support = yield prettier.getFileInfo(systemPath, options);
            if (support.ignored || !support.inferredParser) {
                return;
            }
            try {
                host.overwrite(file.path, prettier.format(file.content, options));
            }
            catch (e) {
                context.logger.warn(`Could not format ${file.path} because ${e.message}`);
            }
        })), operators_1.map(() => host));
    };
}
exports.formatFiles = formatFiles;
function updateWorkspaceJsonToMatchFormatVersion(host, directory) {
    const possibleFiles = [
        `${directory}/workspace.json`,
        `${directory}/angular.json`,
    ];
    const path = possibleFiles.filter((path) => host.exists(path))[0];
    try {
        if (path) {
            const workspaceJson = devkit_1.parseJson(host.read(path).toString());
            const reformatted = workspace_1.reformattedWorkspaceJsonOrNull(workspaceJson);
            if (reformatted) {
                host.overwrite(path, devkit_1.serializeJson(reformatted));
            }
        }
    }
    catch (e) {
        console.error(`Failed to format: ${path}`);
        console.error(e);
    }
}
//# sourceMappingURL=format-files.js.map